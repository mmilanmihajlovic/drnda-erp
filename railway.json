{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "releaseCommand": "php artisan migrate --force && php artisan db:seed --force && php artisan storage:link --force",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 60
  }
}
