{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "releaseCommand": "php artisan migrate --force; php artisan db:seed --force || true; php artisan storage:link --force || true",
    "healthcheckPath": "/up",
    "healthcheckTimeout": 60
  }
}
